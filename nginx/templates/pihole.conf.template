server {
    listen 80 default_server;
    server_name ${PIHOLE_HOST} _;

    location / {
        proxy_pass http://pihole:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
