# Copy this to .env and fill in your values
# .env is gitignored — never commit it

TZ=America/Denver
HOSTNAME=malphas
DOMAIN=ct.home

# Site hostnames (add more as you add services)
CT_SITE_HOST=conspicuoustechnologist.ct.home
